# PostgreSQL Database Configuration for STAGING
DB_USER=staging_user
DB_PASSWORD=staging_password_2024
DB_HOST=localhost
DB_PORT=5432
DB_NAME=material_management_staging

# Environment
NODE_ENV=staging

# Session Configuration
SESSION_SECRET=staging_session_secret_change_in_production

# Server Configuration
PORT=5001

# CORS Origin (adjust for staging frontend)
CORS_ORIGIN=http://localhost:3000